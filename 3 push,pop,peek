#include <stdio.h>
#include <stdlib.h>

#define MAX 5

int stack[MAX];
int top = -1;

void push(int val)
{
    if (top == MAX - 1)
        printf("\nStack Overflow! Cannot push %d\n", val);
    else
        stack[++top] = val;
}

void pop()
{
    if (top == -1)
        printf("\nStack Underflow!\n");
    else
        printf("\n%d popped from stack\n", stack[top--]);
}

void peek()
{
    if (top == -1)
        printf("\nStack is empty.\n");
    else
        printf("\nTop element is: %d\n", stack[top]);
}

void display()
{
    if (top == -1)
        printf("\nStack is empty.\n");
    else {
        printf("\nStack elements:\n");
        for (int i = top; i >= 0; i--)
            printf("%d\n", stack[i]);
    }
}

int main()
{
    int choice, val;

    while (1)
    {
        printf("\n--- STACK MENU ---");
        printf("\n1. Push");
        printf("\n2. Pop");
        printf("\n3. Peek");
        printf("\n4. Display");
        printf("\n5. Exit");
        printf("\nEnter your choice: ");
        scanf("%d", &choice);

        switch (choice)
        {
            case 1:
                printf("Enter value to push: ");
                scanf("%d", &val);
                push(val);
                break;
            case 2:
                pop();
                break;
            case 3:
                peek();
                break;
            case 4:
                display();
                break;
            case 5:
                printf("\nExiting program...\n");
                return 0;
            default:
                printf("\nInvalid choice!\n");
        }
    }
}

algorithm

1.Start
2.Initialize
-Create an array stack[MAX]
-Set top = -1 (means stack is empty)
3.Repeat the menu until the user exits
Show the menu:
Push
Pop
Peek
Display
Exit
Take the user's choice.
--Push Operation--
4.If user selects Push:
-Read the value
-If top == MAX - 1
-Print "Stack Overflow"
-Else
-Increase top by 1
-Insert the value at stack[top]
--Pop Operation--
5.If user selects Pop:
-If top == -1
-Print "Stack Underflow"
-Else
-Print and remove the value at stack[top]
-Decrease top by 1
--Peek Operation--
6.If user selects Peek:
-If top == -1
-Print "Stack is empty"
-Else
-Print the value at stack[top]
--Display Opeartion--
7.If user selects Display:
-If top == -1
-Print "Stack is empty"
-Else
-Print all elements from top down to 0
--Exit--
8.If user selects Exit:
-Stop the program
