Q4)Implement enqueue and dequeue operations using queue data structure.
#include <stdio.h>
#include <conio.h>
#include <stdlib.h>

#define MAX 5   // Maximum size of the queue

int queue[MAX];
int front = -1, rear = -1;

// Function to insert (enqueue) an element
void enqueue(int val)
{
    if (rear == MAX - 1)
    {
        printf("\nQueue Overflow! Cannot insert %d\n", val);
    }
    else
    {
        if (front == -1)
            front = 0;
        rear++;
        queue[rear] = val;
        printf("\n%d inserted into queue\n", val);
    }
}

// Function to delete (dequeue) an element
void dequeue()
{
    if (front == -1 || front > rear)
    {
        printf("\nQueue Underflow! No elements to delete.\n");
    }
    else
    {
        printf("\n%d deleted from queue\n", queue[front]);
        front++;
    }
}

// Function to display the queue elements
void display()
{
    int i;
    if (front == -1 || front > rear)
    {
        printf("\nQueue is empty.\n");
    }
    else
    {
        printf("\nQueue elements are:\n");
        for (i = front; i <= rear; i++)
        {
            printf("%d ", queue[i]);
        }
        printf("\n");
    }
}

void main()
{
    int choice, val;
    clrscr();  // Clear screen (Turbo C specific)

    while (1)
    {
        printf("\n--- QUEUE MENU ---");
        printf("\n1. Enqueue (Insert)");
        printf("\n2. Dequeue (Delete)");
        printf("\n3. Display");
        printf("\n4. Exit");
        printf("\nEnter your choice: ");
        scanf("%d", &choice);

        switch (choice)
        {
        case 1:
            printf("Enter value to insert: ");
            scanf("%d", &val);
            enqueue(val);
            break;

        case 2:
            dequeue();
            break;

        case 3:
            display();
            break;

        case 4:
            printf("\nExiting program...");
            getch();
            exit(0);

        default:
            printf("\nInvalid choice! Try again.\n");
        }
    }
}

Algorithm

Start
2. Initialize
Create an array queue[MAX]
Set front = -1
Set rear = -1
3. Repeat the menu until the user exits
Display menu:
Enqueue
Dequeue
Display
Exit
Take user choice.
✅ Enqueue Operation (Inset)
4. If user selects Enqueue:
Read the value
Check if rear == MAX - 1
If yes → print "Queue Overflow" (Queue is full)
Else:
If front == -1, set front = 0
Increase rear by 1
Store the value in queue[rear]
Print that value is inserted
✅ Dequeue Operation (Delete)
5. If user selects Dequeue:
Check if queue is empty:
If front == -1 OR front > rear
Print "Queue Underflow" (No elements to delete)
Else:
Print the value at queue[front]
Increase front by 1
✅ Display Operation
6. If user selects Display:
Check if queue is empty:
If front == -1 OR front > rear
Print "Queue is empty"
Else:
Print all elements from front to rear
✅ Exit Operation7. If user selects Exit:
Stop the program
8. End
