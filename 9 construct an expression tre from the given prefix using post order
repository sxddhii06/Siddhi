9. Write a program in C to Construct an expression tree from the given
prefix expression, e.g., +--a*bc/def, traverse it using post-order
traversal.
#include <stdio.h>
#include <conio.h>
#include <stdlib.h>
#include <string.h>

struct Node {
    char data;
    struct Node *left, *right;
};

struct Node* createNode(char ch) {
    struct Node *newNode;
    newNode = (struct Node*)malloc(sizeof(struct Node));
    newNode->data = ch;
    newNode->left = newNode->right = NULL;
    return newNode;
}

struct Node* buildTree(char *prefix, int *index) {
    char ch = prefix[*index];
    (*index)++;
    if(ch == '\0') return NULL;

    struct Node *node = createNode(ch);

    if(ch == '+' || ch == '-' || ch == '*' || ch == '/' || ch == '^') {
        node->left = buildTree(prefix, index);
        node->right = buildTree(prefix, index);
    }
    return node;
}

void postOrder(struct Node *root) {
    if(root == NULL) return;
    postOrder(root->left);
    postOrder(root->right);
    printf("%c ", root->data);
}

void main() {
    char prefix[50] = "+--a*bc/def";
    int index = 0;
    struct Node *root;
    clrscr();

    root = buildTree(prefix, &index);
    printf("Postorder Traversal: ");
    postOrder(root);

    getch();
}

Algorithm

Step 1: Start
Begin the program.
Step 2: Read/Store the prefix expression
Example: +--a*bc/def
Step 3: Create a function to build the tree
Read one character from the prefix string.
Create a new node for that character.
If the character is an operator (+ - * / ^)
Recursively create left child
Recursively create right child
If the character is an operand (a, b, c, d, e, f)
Make it a leaf node.
Step 4: Continue recursion until full expression is processed
Keep increasing index after every character.
Stop when end of string is reached.
Step 5: After tree is built successfully, perform post-order traversal
Post-order =Left → Right → Root
Steps:
Visit left subtree
Visit right subtree
Print the root
Step 6: Display the output
Print all characters in post-order format
Step 7: Stop
